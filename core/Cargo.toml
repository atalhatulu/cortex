[package]
name = "cortex"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "=4.4.18", features = ["derive"] }
divsufsort = "2.0.0"
rayon = "1.8"

[profile.release]
opt-level = 3
lto = true

[dev-dependencies]
criterion = "0.5.1"

[[bench]]
name = "compression_bench"
harness = false
